--extra-index-url https://download.pytorch.org/whl/cu124
# Pinned exactly (not a floor): modiff_cutlass is a prebuilt CUDAExtension (see setup.py)
# linked against this specific torch build via CUDA 12.4 (nvcc at /usr/local/cuda-12.4,
# CUTLASS at /workspace/cutlass). An unpinned floor here lets pip resolve a newer/ABI-
# incompatible torch (e.g. cu13x) on a fresh install, which silently breaks the already
# -built modiff_cutlass.so ("CUTLASS backend not available") until torch is reinstalled
# at this exact version and the extension is rebuilt. If you bump this, rebuild the
# extension (`pip install .` with CUTLASS_PATH set) against the new torch/CUDA pair.
torch==2.4.1+cu124
torchvision==0.19.1+cu124
torchaudio==2.4.1+cu124
triton>=2.1.0
numpy>=1.26.3
albumentations==0.4.3
diffusers==0.3.0
pudb==2019.2
invisible-watermark
imageio==2.9.0
imageio-ffmpeg==0.4.2
test-tube>=0.7.5
blinker>=1.5
torch-fidelity==0.3.0
torchmetrics==0.6.0
streamlit-drawable-canvas==0.8
einops==0.3.0
kornia==0.6.9
lmdb==1.3.0
natsort==8.3.1
omegaconf==2.1.1
opencv-python
opencv-python-headless
pandas
Pillow>=9.3.0
pytorch-lightning==1.4.2
PyYAML>=6.0.1
six==1.16.0
tqdm==4.64.0
transformers>=4.22.2
omegaconf
git+https://github.com/openai/CLIP.git@main#egg=clip
git+https://github.com/CompVis/taming-transformers.git@master#egg=taming-transformers

# Build dependency for the CUDA extension. Without it torch's BuildExtension silently falls
# back to the serial distutils backend, which turned a rebuild of the 14 CUTLASS translation
# units from ~minutes into >10 minutes on a 96-core box (measured 2026-08-03).
ninja
